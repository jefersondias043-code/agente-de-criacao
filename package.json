{
  "name": "agente-de-postagem",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agente de Postagem - PWA client-side para criação de matérias com IA",
  "scripts": {
    "lint": "eslint \"src/**/*.js\"",
    "test": "vitest run",
    "check:manifest": "node scripts/check-manifest.mjs",
    "sync:manifest": "node scripts/sync-manifest.mjs",
    "bump:version": "node scripts/bump-version.mjs",
    "verify": "npm run lint && npm run test && npm run check:manifest && node scripts/sync-manifest.mjs --check"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "globals": "^15.14.0",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.8"
  }
}
